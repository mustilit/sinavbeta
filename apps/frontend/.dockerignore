# Prevent host node_modules from overwriting container's npm ci output
node_modules
# Build output and caches
dist
.vite
*.local
.env*.local
# Git and IDE
.git
.gitignore
.cursor
*.md
# Runtime/test artifacts + tool home-junk (build context'e girmemeli; bazilari
# root sahipli olup context transfer'ini kirar — orn. chromium'un .pki dizini)
.pki
.cache
.config
.mozilla
.local
.npm
playwright-report
test-results
.lighthouseci
e2e
