# Staging override.
#
# Not: docker-compose.prod.yml artık TEK BAŞINA deploy edilebilir — DOCKER=1,
# worker/dlq env_file, frontend port (80:5173) ve tüm tırnaksız default'lar
# prod compose'a taşındı. Bu yüzden burada artık zorunlu bir override YOK.
#
# Bu dosya yalnızca staging'e ÖZGÜ farklar için ayrılmıştır (örn. staging'de
# farklı bir host portu istenirse: `FRONTEND_HTTP_PORT` env veya aşağıya
# frontend.ports override'ı). Şu an ek fark olmadığından boş bırakıldı.
services: {}
