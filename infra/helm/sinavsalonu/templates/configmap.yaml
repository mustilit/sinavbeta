apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sinavsalonu.fullname" . }}-backend-env
  labels:
    {{- include "sinavsalonu.labels" . | nindent 4 }}
data:
{{- range $k, $v := .Values.backend.env }}
  {{ $k }}: {{ $v | quote }}
{{- end }}
